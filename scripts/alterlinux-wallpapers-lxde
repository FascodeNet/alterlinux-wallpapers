#!/usr/bin/env bash

image="/usr/share/lxde/wallpapers/lxde_${1}.jpg"
replace="/usr/share/backgrounds/${2}.png"

# Replace wallpaper.
if [[ -f "${image}" ]] && [[ -f "${replace}" ]]; then
    rm -rf "${image}"
    ln -s "${replace}" "${image}"
    chmod 644 "${replace}"
fi
